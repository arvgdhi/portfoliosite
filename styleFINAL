/* Space Futuristic Monotype Theme (Solar Edition) */
:root {
    --bg-color: #030303; /* Deep space black */
    --surface-color: #0a0a0a;
    --text-main: #f0f0f0; 
    --text-muted: #888888;
    --accent: #ff4500; /* Blazing Hot Orange-Red */
    --accent-dim: rgba(255, 69, 0, 0.2);
    
    --font-mono: 'Space Mono', monospace;
    
    --transition-fast: 0.15s ease;
    --transition-medium: 0.3s ease-out;
}

/* Base Reset & Cursor hiding */
*, *::before, *::after {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    cursor: none !important; 
}

html {
    scroll-behavior: smooth;
    font-size: 12px; /* Scaled down ~75% of default 16px */
}

body {
    background-color: var(--bg-color);
    color: var(--text-main);
    font-family: var(--font-mono);
    line-height: 1.6;
    overflow-x: hidden;
    position: relative;
    /* Basic grid over the stars for a HUD feel */
    background-image: linear-gradient(rgba(255,255,255,0.02) 1px, transparent 1px),
                      linear-gradient(90deg, rgba(255,255,255,0.02) 1px, transparent 1px);
    background-size: 50px 50px;
}

/* 
 * Animated CSS Stars 
 * We use box-shadows on two distinct layers to simulate deep space 
 */
#stars-container, #stars-container-2 {
    position: fixed;
    top: 0; left: 0;
    width: 2px; height: 2px;
    background: transparent;
    z-index: -2;
    pointer-events: none;
}

/* Let's generate a bunch of box-shadow coordinates for a scattered field.
   Instead of hardcoding hundreds, we'll use a CSS repeating radial gradient trick for performance 
   since we don't have SCSS to generate random box-shadows easily. */

body::after {
    content: '';
    position: fixed;
    top: 0; left: 0; width: 200vw; height: 200vh;
    background-image: radial-gradient(1px 1px at 10% 10%, #fff 100%, transparent),
                      radial-gradient(1.5px 1.5px at 25% 30%, #fff 100%, transparent),
                      radial-gradient(1px 1px at 40% 80%, #fff 100%, transparent),
                      radial-gradient(2px 2px at 60% 20%, #fff 100%, transparent),
                      radial-gradient(1px 1px at 80% 50%, #fff 100%, transparent),
                      radial-gradient(1px 1px at 90% 90%, #fff 100%, transparent),
                      radial-gradient(1px 1px at 5% 50%, #fff 100%, transparent),
                      radial-gradient(2px 2px at 30% 60%, var(--accent) 100%, transparent), /* occasional gold star */
                      radial-gradient(1px 1px at 70% 80%, #fff 100%, transparent);
    background-size: 300px 300px;
    opacity: 0.3;
    z-index: -2;
    pointer-events: none;
    animation: slow-drift 60s linear infinite;
}

@keyframes slow-drift {
    from { transform: translateY(0); }
    to { transform: translateY(-50vh); }
}


/* Custom Viewfinder Cursor */
#custom-cursor {
    position: fixed;
    top: 0;
    left: 0;
    width: 14px;
    height: 14px;
    background-color: var(--accent);
    transform: translate(-50%, -50%); /* the origin moves smoothly */
    pointer-events: none;
    z-index: 9999;
    will-change: width, height, transform, top, left;
    transition: width 0.25s cubic-bezier(0.175, 0.885, 0.32, 1.275), 
                height 0.25s cubic-bezier(0.175, 0.885, 0.32, 1.275),
                background-color 0.2s ease,
                transform 0.25s ease;
    mix-blend-mode: exclusion;
}

/* When hover triggers Target Mode (Camera Brackets) */
#custom-cursor.target-mode {
    background-color: transparent;
    transform: translate(0, 0); 
    
    /* Using multiple linear gradients to draw only the 4 corners (camera viewfinder) */
    /* top-left horizontal, top-left vertical, etc... */
    background-image: 
        linear-gradient(var(--accent), var(--accent)),
        linear-gradient(var(--accent), var(--accent)),
        linear-gradient(var(--accent), var(--accent)),
        linear-gradient(var(--accent), var(--accent)),
        linear-gradient(var(--accent), var(--accent)),
        linear-gradient(var(--accent), var(--accent)),
        linear-gradient(var(--accent), var(--accent)),
        linear-gradient(var(--accent), var(--accent));
    
    background-repeat: no-repeat;
    
    /* size: width height */
    /* thickness 2px, length 15px */
    background-size: 
        15px 2px, 2px 15px, /* TL */
        15px 2px, 2px 15px, /* TR */
        15px 2px, 2px 15px, /* BL */
        15px 2px, 2px 15px; /* BR */
        
    /* position: x y */
    background-position: 
        0 0, 0 0,           /* TL */
        100% 0, 100% 0,     /* TR */
        0 100%, 0 100%,     /* BL */
        100% 100%, 100% 100%; /* BR */
}


/* Typography (LARGE FONT OVERHAUL) */
h1, h2, h3 {
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: -2px;
}

.main-heading {
    font-size: clamp(3.5rem, 10vw, 8rem); /* Slightly shrunk to fit Aarav Gandhi smoothly */
    color: var(--text-main);
    line-height: 0.9;
    margin-bottom: 2rem;
    position: relative;
    display: inline-block;
}

/* Slight glitch/futuristic shadow effect */
.main-heading::before {
    content: '';
    position: absolute;
    width: 100%; height: 50%;
    bottom: 0; left: 0;
    background: var(--bg-color);
    z-index: -1;
    transform: skewX(-15deg);
}

.section-title {
    font-size: clamp(2.2rem, 5vw, 3.5rem);
    color: var(--text-main);
    margin-bottom: 4rem;
    border-bottom: 2px solid var(--text-muted);
    padding-bottom: 1rem;
    display: inline-block;
}

.section-title::first-letter {
    color: var(--accent);
}

/* Navigation */
nav {
    position: fixed;
    top: 0;
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 2.5rem 5%;
    z-index: 100;
    background: transparent;
    mix-blend-mode: difference;
}

.logo {
    font-size: 2rem;
    font-weight: 700;
    color: var(--accent);
    letter-spacing: -1px;
}

.nav-links {
    display: flex;
    list-style: none;
    gap: 3rem;
}

.nav-links a {
    color: var(--text-main);
    text-decoration: none;
    font-size: 1.1rem;
    text-transform: uppercase;
    transition: color var(--transition-medium);
}

.nav-links a:hover {
    color: var(--accent);
}

/* Layout Utilities */
.section {
    min-height: 100vh;
    padding: 12rem 5% 8rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
}

.container {
    max-width: 1400px;
    margin: 0 auto;
    width: 100%;
}

.mt-5 { margin-top: 5rem; }
.mt-8 { margin-top: 8rem; }

/* Buttons */
.btn {
    display: inline-block;
    padding: 1.5rem 3rem;
    color: var(--bg-color);
    background-color: var(--accent);
    text-decoration: none;
    font-size: 1.3rem;
    font-weight: 700;
    transition: var(--transition-medium);
    border: 2px solid var(--accent);
    text-align: center;
    position: relative;
}

.btn:hover {
    background-color: transparent;
    color: var(--accent);
    box-shadow: inset 0 0 15px var(--accent-dim);
}

/* Hero Section */
.hero {
    align-items: flex-start;
    text-align: left;
    position: relative;
    overflow: hidden;
}

#solar-flare {
    position: absolute;
    top: 50%;
    right: -25vw;
    width: clamp(375px, 45vw, 900px); /* Scaled ~75% */
    height: auto;
    mix-blend-mode: screen;
    pointer-events: none;
    z-index: -1;
    opacity: 0.7;
    transform: translateY(-50%) rotate(-90deg); /* Static orientation */
}

.hero-content {
    z-index: 2;
    position: relative;
}

.sub-heading {
    color: var(--accent);
    font-size: clamp(1.2rem, 2.5vw, 2rem);
    margin-bottom: 1rem;
    font-weight: 400;
}

.tagline {
    font-size: clamp(1.2rem, 3vw, 2rem);
    color: var(--text-main);
    margin-bottom: 2rem;
    letter-spacing: 0;
    max-width: 900px;
    font-weight: 700;
}

.intro-text {
    font-size: 1.15rem;
    color: var(--text-muted);
    margin-bottom: 4rem;
    max-width: 800px;
}



/* About Section */
.about-content {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 6rem;
}

.about-text {
    font-size: 1.2rem;
    color: var(--text-muted);
}

.about-text p {
    margin-bottom: 2rem;
}

.about-text strong {
    color: var(--text-main);
}

.about-text h3, .skills-container h3, .achievements-bar h3, .goal-box h3 {
    font-size: 1.8rem;
    margin-bottom: 2rem;
    color: var(--accent);
}

.skills-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 1.2rem;
}

.skill-tag {
    padding: 1rem 1.5rem;
    border: 1px solid var(--text-muted);
    font-size: 1rem;
    color: var(--text-main);
    transition: var(--transition-medium);
    text-align: center;
    background: rgba(10, 10, 10, 0.8);
}

.skill-tag:hover {
    border-color: var(--accent);
    color: var(--accent);
    box-shadow: 0 0 10px var(--accent-dim);
}

/* Milestones Grid */
.milestones-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 2rem;
}
.milestone-card {
    background: rgba(10, 10, 10, 0.8);
    border: 1px solid var(--text-muted);
    padding: 2.5rem 2rem;
    font-size: 1.1rem;
    color: var(--text-main);
    transition: var(--transition-medium);
}
.milestone-card:hover {
    border-color: var(--accent);
    box-shadow: 0 0 15px var(--accent-dim);
}

/* Projects Grid */
#projects {
    position: relative;
}

#icarus-falling {
    position: absolute;
    right: 5%;
    width: clamp(37px, 6vw, 90px); /* Scaled ~75% */
    height: auto;
    mix-blend-mode: screen;
    pointer-events: none;
    z-index: -1;
    animation: fall-from-grace 35s linear infinite; /* Slowed down substantially */
}

@keyframes fall-from-grace {
    0% { top: -10%; transform: rotate(-5deg); opacity: 0; }
    5% { opacity: 0.8; }
    85% { opacity: 0.8; }
    100% { top: 110%; transform: rotate(20deg); opacity: 0; }
}

.projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    gap: 6rem;
}

.project-card {
    background-color: rgba(10, 10, 10, 0.9);
    border: 1px solid var(--text-muted);
    padding: 4rem 3rem;
    transition: var(--transition-medium);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    min-height: 380px;
    position: relative;
    overflow: hidden;
    word-break: break-word; /* Prevents long monotype text from poking out */
    text-decoration: none; /* Required since it's an <a> tag now */
    color: inherit;
    cursor: none !important;
}

.project-card::after {
    content: '';
    position: absolute;
    top: 0; right: 0;
    width: 30px; height: 30px;
    background: linear-gradient(-135deg, var(--accent) 50%, transparent 50%);
    opacity: 0;
    transition: opacity var(--transition-medium);
}

.project-card:hover {
    border-color: var(--accent);
    transform: translateY(-5px);
    box-shadow: 0 10px 30px rgba(0,0,0,0.8);
}

.project-card:hover::after {
    opacity: 1;
}

.project-content h3 {
    font-size: 1.6rem;
    margin-bottom: 1.5rem;
    color: var(--text-main);
}

.project-content p {
    color: var(--text-muted);
    margin-bottom: 3rem;
    font-size: 1.1rem;
}



/* Research Section */
.research-list {
    display: flex;
    flex-direction: column;
    gap: 3rem;
}

.research-card {
    background-color: rgba(10, 10, 10, 0.9);
    border: 1px solid var(--text-muted);
    padding: 4rem 3rem;
    transition: var(--transition-medium);
    position: relative;
    overflow: hidden;
}

.research-card::after {
    content: '';
    position: absolute;
    top: 0; right: 0;
    width: 30px; height: 30px;
    background: linear-gradient(-135deg, var(--accent) 50%, transparent 50%);
    opacity: 0;
    transition: opacity var(--transition-medium);
}

.research-card:hover {
    border-color: var(--accent);
    transform: translateY(-5px);
    box-shadow: 0 10px 30px rgba(0,0,0,0.8);
}

.research-card:hover::after {
    opacity: 1;
}

.research-card h3 {
    font-size: 1.8rem;
    color: var(--text-main);
    margin-bottom: 1rem;
}

.research-card p {
    font-size: 1.2rem;
    color: var(--text-muted);
    max-width: 1000px;
}

/* Footer / Contact */
footer {
    border-top: 1px solid var(--accent-dim);
    position: relative;
}

.goals-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    background: rgba(255, 69, 0, 0.03);
    padding: 3rem;
    border: 1px solid var(--accent-dim);
}

.goal-box p {
    color: var(--text-muted);
    font-size: 1.2rem;
}

.contact-text {
    font-size: 1.5rem;
    color: var(--text-main);
    margin-bottom: 3rem;
}

.social-links {
    display: flex;
    flex-wrap: wrap;
    gap: 2rem;
    margin-bottom: 3rem;
}

.social-links a {
    color: var(--text-main);
    text-decoration: none;
    font-size: 1.3rem;
    padding: 1.2rem 2.5rem;
    border: 1px solid var(--accent-dim);
    background: rgba(10, 10, 10, 0.8);
    transition: var(--transition-medium);
}

.social-links a:hover {
    color: var(--accent);
    border-color: var(--accent);
    box-shadow: 0 0 15px var(--accent-dim);
    transform: translateY(-3px);
}

.copyright {
    color: var(--text-muted);
    font-size: 1rem;
}

/* Auto-hide for scroll reveal */
.auto-hide {
    opacity: 0;
    transform: translateY(50px);
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
}

.auto-hide.show {
    opacity: 1;
    transform: translateY(0);
}

/* Responsive */
@media (max-width: 1000px) {
    .about-content, .goals-container {
        grid-template-columns: 1fr;
        gap: 5rem;
    }
    
    .main-heading {
        font-size: 5rem;
    }
    
    .nav-links {
        display: none;
    }
}
